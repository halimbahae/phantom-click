[package]
name = "phantom-click"
version = "0.1.0"
edition = "2021"
description = "A hacker-terminal CPS test with keyboard shortcuts"

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"
